# Local development overrides for necrobloom.
# Copy to .env (gitignored). Do NOT set VITE_API_URL to a port —
# leave it empty so Vite's proxy handles /api/* → localhost:7072
# and injects the mock x-ms-client-principal auth header.
VITE_API_URL=
