name: Deploy NecroBloom Backend

on:
  push:
    branches:
      - main
    paths:
      - 'apps/necrobloom/api/**'
      - '.github/workflows/deploy-necrobloom-backend.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'apps/necrobloom/api/**'
      - '.github/workflows/deploy-necrobloom-backend.yml'
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_NAME: 'necrobloom-api'
  JAVA_VERSION: '21'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./apps/necrobloom/api
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'gradle'

      - name: Build and Test with Gradle
        run: ./gradlew clean build azureFunctionsPackage

      - name: Deploy to Azure Functions
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
          package: './apps/necrobloom/api/build/azure-functions/${{ env.AZURE_FUNCTIONAPP_NAME }}'
          publish-profile: ${{ secrets.AZURE_NECROBLOOM_API_PUBLISH_PROFILE }}
